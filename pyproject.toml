[tool.poetry]
name = "netlify-python"
version = "0.1.0"
description = ""
authors = ["Chris Brousseau <cbrews@users.noreply.github.com>"]
readme = "README.md"
packages = [
  {include = "netlify"}
]

[tool.poetry.dependencies]
python = "^3.10"
httpx = "^0.23.0"
python-dateutil = "^2.8.2"

[tool.poetry.group.dev.dependencies]
black = "^22.10.0"
mypy = "^0.982"
pylint = "^2.15.4"
isort = "^5.10.1"
pytest = "^7.1.3"
types-python-dateutil = "^2.8.19.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.isort]
profile = "black"